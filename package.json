{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.8",
    "typescript": "^5.1.6"
  },
  "name": "turbo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
