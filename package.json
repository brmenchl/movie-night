{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "validate": "turbo run typecheck lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,js,json}\"",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@evilmartians/lefthook": "^1.5.0",
    "eslint": "^8.49.0",
    "prettier": "^3.0.3",
    "turbo": "^1.10.14",
    "typescript": "^5.2.2"
  },
  "name": "turbo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
